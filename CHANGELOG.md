# Changelog

## 0.1.0 - 2026-09-03

- Add opaque, Session- and project-bound Process Handle issuance.
- Detect real process exit without treating PID reuse as an exit.
- Expose only the read-only `process.status` custom Bundle capability.
